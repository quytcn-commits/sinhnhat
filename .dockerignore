node_modules
.next
.git
.gitignore
README.md
Dockerfile
.dockerignore
docker-compose.yml
npm-debug.log*

# Tài liệu thiết kế & file nguồn (KHÔNG đưa vào image)
*.psb
*.rar
*.woff
1FTV-*.otf
design-src
Layout NS*.psb

# File Excel nhân sự (NHẠY CẢM — không đưa vào image; data đã import sẵn vào data/employees.json)
*.xlsx
*.xls

# file tạm
_*.png
_*.log

# data runtime (dùng volume, không build vào image)
runtime-data
design
